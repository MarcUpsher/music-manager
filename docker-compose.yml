version: "3"

networks:
  overlay:
    driver: bridge

services:
    web:
        build: 
            context: .
            dockerfile: MusicManager.Web.API/Dockerfile
        networks:
            - overlay
        ports:
            - "8000:80"
        depends_on:
            - sql-server-db
    sql-server-db:
        image: "mcr.microsoft.com/mssql/server"
        networks:
            - overlay
        environment:
            SA_PASSWORD: "mm.p@ss1234!#"
            ACCEPT_EULA: "Y"
        ports: 
            - "1433:1433"
